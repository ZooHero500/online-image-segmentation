# SEO 修复实施计划

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复 SEO-AUDIT.md 中列出的所有 Critical/High/Medium 优先级 SEO 问题，将 SEO 就绪度从 ~40 提升到 ~85。

**Architecture:** 主要修改 Next.js App Router 层的元数据配置、页面渲染方式和新增 SEO 基础设施文件。不涉及业务逻辑变更。

**Tech Stack:** Next.js 16 (App Router), next-intl, TypeScript

---

## 文件结构

| 操作 | 文件路径 | 职责 |
|------|----------|------|
| **重构** | `src/app/[locale]/page.tsx` | 改为 Server Component，提取客户端交互组件 |
| **创建** | `src/app/[locale]/FaqSection.tsx` | FAQ 展开/收起交互（"use client"） |
| **创建** | `src/app/robots.ts` | 爬虫抓取规则 |
| **创建** | `src/app/sitemap.ts` | XML 站点地图 |
| **修改** | `src/app/[locale]/layout.tsx` | 添加 OG/Twitter/canonical 元数据 |
| **创建** | `src/components/JsonLd.tsx` | JSON-LD 结构化数据组件 |
| **创建** | `src/app/[locale]/workspace/layout.tsx` | workspace noindex 元数据 |
| **修改** | `src/messages/en.json` | 优化标题文案 |
| **修改** | `src/messages/zh-CN.json` | 优化标题文案 |

---

## Chunk 1: 首页 Server Component 重构（Critical）

### Task 1: 将首页从 Client Component 重构为 Server Component

**目标：** 移除 `page.tsx` 的 `"use client"`，使所有静态 HTML（标题、section、FAQ 问答）在服务端渲染，搜索引擎可直接读取。

**Files:**
- Refactor: `src/app/[locale]/page.tsx` (移除 "use client"，改用 server 翻译)
- Create: `src/app/[locale]/FaqSection.tsx` (客户端交互：展开/收起)
- Keep: `src/app/[locale]/LandingContent.tsx` (已是独立 client component)

- [ ] **Step 1: 创建 FaqSection 客户端组件**

```tsx
// src/app/[locale]/FaqSection.tsx
"use client"

import { useState } from "react"
import { Plus } from "lucide-react"

export function FaqItem({
  question,
  answer,
}: {
  question: string
  answer: string
}) {
  const [isOpen, setIsOpen] = useState(false)

  return (
    <div className="border-t border-[#1A1A1A]/10">
      <button
        onClick={() => setIsOpen(!isOpen)}
        className="w-full flex items-start justify-between gap-4 py-6 text-left group"
      >
        <h3
          className={`text-sm font-medium transition-colors duration-500 ${
            isOpen ? "text-[#D4AF37]" : "text-[#1A1A1A] group-hover:text-[#D4AF37]"
          }`}
        >
          {question}
        </h3>
        <div
          className={`shrink-0 w-6 h-6 border flex items-center justify-center transition-all duration-500 ${
            isOpen
              ? "border-[#D4AF37] rotate-45"
              : "border-[#1A1A1A]/20 rotate-0 group-hover:border-[#D4AF37]"
          }`}
        >
          <Plus className="h-3 w-3 text-[#1A1A1A]" strokeWidth={1.5} />
        </div>
      </button>
      {isOpen && (
        <div className="pb-6 animate-fade-in-down">
          <p className="text-sm text-[#6C6863] leading-relaxed">{answer}</p>
        </div>
      )}
    </div>
  )
}
```

- [ ] **Step 2: 重构 page.tsx 为 Server Component**

将 `src/app/[locale]/page.tsx` 重写：
- 移除第1行的 `"use client"`
- 将 `useState` 的 import 移除
- 将 `useTranslations` 替换为 `getTranslations`（server 端）
- 将组件函数改为 `async function`
- 将 `FaqItem` 移至独立的 `FaqSection.tsx` 并 import
- `LandingContent` 已是独立 client component，无需改动
- `GridLines`、`TrustItem`、`BenefitCard`、`FeatureCard`、`StepCard`、`UseCaseCard` 均无状态，保留在 page.tsx 内作为 Server Component 子组件

关键改动摘要：
```diff
- "use client"
-
- import { useState } from "react"
- import { useTranslations } from "next-intl"
+ import { getTranslations, setRequestLocale } from "next-intl/server"
+ import { FaqItem } from "./FaqSection"

- export default function Home() {
-   const t = useTranslations()
+ export default async function Home({ params }: { params: Promise<{ locale: string }> }) {
+   const { locale } = await params
+   setRequestLocale(locale)
+   const t = await getTranslations()
```

其余 JSX 结构保持完全不变，只是 `FaqItem` 改为从外部导入。

- [ ] **Step 3: 验证构建通过**

Run: `bun run build`
Expected: 构建成功，首页 HTML 包含所有文本内容

- [ ] **Step 4: 验证 SSR 输出包含关键内容**

Run: `bun run build && bun run start`
然后在另一个终端：
```bash
curl -s http://localhost:3000 | grep -c "Precision"
```
Expected: 输出 ≥ 1（说明 H1 文本在 HTML 中存在）

- [ ] **Step 5: Commit**

```bash
git add src/app/[locale]/page.tsx src/app/[locale]/FaqSection.tsx
git commit -m "refactor(seo): 首页改为 Server Component，确保 SSR 输出完整 HTML"
```

---

## Chunk 2: 基础 SEO 设施

### Task 2: 创建 robots.ts

**Files:**
- Create: `src/app/robots.ts`

- [ ] **Step 1: 创建 robots.ts**

```typescript
// src/app/robots.ts
import type { MetadataRoute } from "next"

const BASE_URL = process.env.NEXT_PUBLIC_SITE_URL || "https://imgsplit.com"

export default function robots(): MetadataRoute.Robots {
  return {
    rules: [
      {
        userAgent: "*",
        allow: "/",
        disallow: ["/workspace"],
      },
    ],
    sitemap: `${BASE_URL}/sitemap.xml`,
  }
}
```

- [ ] **Step 2: 验证构建通过**

Run: `bun run build`
Expected: 构建成功，无错误

- [ ] **Step 3: Commit**

```bash
git add src/app/robots.ts
git commit -m "feat(seo): 添加 robots.ts 爬虫规则，禁止抓取 /workspace"
```

---

### Task 3: 创建 sitemap.ts

**Files:**
- Create: `src/app/sitemap.ts`
- Read: `src/i18n/routing.ts` (获取 locale 列表)

- [ ] **Step 1: 创建 sitemap.ts**

```typescript
// src/app/sitemap.ts
import type { MetadataRoute } from "next"
import { routing } from "@/i18n/routing"

const BASE_URL = process.env.NEXT_PUBLIC_SITE_URL || "https://imgsplit.com"

export default function sitemap(): MetadataRoute.Sitemap {
  const entries: MetadataRoute.Sitemap = []

  for (const locale of routing.locales) {
    const url =
      locale === routing.defaultLocale
        ? BASE_URL
        : `${BASE_URL}/${locale}`

    entries.push({
      url,
      lastModified: new Date("2026-03-15"),
      changeFrequency: "weekly",
      priority: 1.0,
      alternates: {
        languages: Object.fromEntries(
          routing.locales.map((l) => [
            l,
            l === routing.defaultLocale ? BASE_URL : `${BASE_URL}/${l}`,
          ])
        ),
      },
    })
  }

  return entries
}
```

> **注意：** `lastModified` 使用固定日期，后续每次内容更新时应手动更新此日期。

- [ ] **Step 2: 验证构建通过**

Run: `bun run build`
Expected: 构建成功，生成 sitemap.xml

- [ ] **Step 3: Commit**

```bash
git add src/app/sitemap.ts
git commit -m "feat(seo): 添加 sitemap.ts 自动生成多语言站点地图"
```

---

### Task 4: workspace 添加 noindex

**Files:**
- Create: `src/app/[locale]/workspace/layout.tsx`

- [ ] **Step 1: 创建 workspace layout**

```typescript
// src/app/[locale]/workspace/layout.tsx
import type { Metadata } from "next"

export const metadata: Metadata = {
  robots: {
    index: false,
    follow: false,
  },
}

export default function WorkspaceLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return <>{children}</>
}
```

- [ ] **Step 2: 验证构建通过**

Run: `bun run build`
Expected: 构建成功

- [ ] **Step 3: Commit**

```bash
git add src/app/[locale]/workspace/layout.tsx
git commit -m "feat(seo): workspace 页面添加 noindex 防止搜索引擎索引"
```

---

## Chunk 3: 元数据增强

### Task 5: 添加 Open Graph / Twitter Card / Canonical

**Files:**
- Modify: `src/app/[locale]/layout.tsx:33-46` (generateMetadata 函数)
- Modify: `src/messages/en.json:2-5` (metadata 部分)
- Modify: `src/messages/zh-CN.json:2-5` (metadata 部分)

**前置条件：** 需要一张 1200×630 的 OG 预览图 `public/og-image.png`。如暂无图片，可先跳过 `images` 字段，后续补充。

- [ ] **Step 1: 优化 en.json 标题**

修改 `src/messages/en.json` 的 metadata 部分：
```json
{
  "metadata": {
    "title": "ImgSplit - Free Online Image Splitting Tool",
    "description": "Free online image splitting tool with drag-and-drop split lines, snap alignment, and instant download. All processing happens in your browser — your images never leave your device."
  }
}
```

- [ ] **Step 2: 优化 zh-CN.json 标题**

修改 `src/messages/zh-CN.json` 的 metadata 部分：
```json
{
  "metadata": {
    "title": "ImgSplit - 免费在线图片分割工具",
    "description": "免费在线图片分割工具，支持拖拽分割线、磁吸对齐、一键生成并下载所有子图片。纯浏览器端处理，保护您的隐私。"
  }
}
```

- [ ] **Step 3: 增强 generateMetadata**

修改 `src/app/[locale]/layout.tsx` 的 `generateMetadata` 函数：

```typescript
const BASE_URL = process.env.NEXT_PUBLIC_SITE_URL || "https://imgsplit.com"

export async function generateMetadata({ params }: { params: Promise<{ locale: string }> }): Promise<Metadata> {
  const { locale } = await params
  const t = await getTranslations({ locale, namespace: "metadata" })

  const canonicalUrl =
    locale === routing.defaultLocale ? BASE_URL : `${BASE_URL}/${locale}`

  return {
    title: t("title"),
    description: t("description"),
    alternates: {
      canonical: canonicalUrl,
      languages: Object.fromEntries(
        routing.locales.map((l) => [
          l,
          l === routing.defaultLocale ? BASE_URL : `${BASE_URL}/${l}`,
        ])
      ),
    },
    openGraph: {
      title: t("title"),
      description: t("description"),
      url: canonicalUrl,
      siteName: "ImgSplit",
      type: "website",
      locale: locale === "zh-CN" ? "zh_CN" : "en_US",
      images: [
        {
          url: `${BASE_URL}/og-image.png`,
          width: 1200,
          height: 630,
          alt: t("title"),
        },
      ],
    },
    twitter: {
      card: "summary_large_image",
      title: t("title"),
      description: t("description"),
      images: [`${BASE_URL}/og-image.png`],
    },
  }
}
```

- [ ] **Step 4: 创建 OG 占位图（如有设计稿可替换）**

将一张 1200×630 的 PNG 图片放到 `public/og-image.png`。可用以下命令生成临时占位图：

```bash
# 如果有 ImageMagick
convert -size 1200x630 xc:#1A1A1A -fill '#D4AF37' -pointsize 72 -gravity center -annotate 0 'ImgSplit' public/og-image.png
```

或手动创建一张品牌 OG 图片。

- [ ] **Step 5: 验证构建通过**

Run: `bun run build`
Expected: 构建成功

- [ ] **Step 6: Commit**

```bash
git add src/app/[locale]/layout.tsx src/messages/en.json src/messages/zh-CN.json public/og-image.png
git commit -m "feat(seo): 添加 Open Graph、Twitter Card、canonical URL 元数据和 OG 图片"
```

---

## Chunk 4: 结构化数据

### Task 6: 添加 JSON-LD 结构化数据

> **依赖：** 必须在 Task 1（首页 Server Component 重构）完成后执行，JSON-LD 需要服务端渲染才能被搜索引擎读取。

**Files:**
- Create: `src/components/JsonLd.tsx`
- Modify: `src/app/[locale]/page.tsx` (引入 JsonLd 组件)

- [ ] **Step 1: 创建 JsonLd 组件**

```tsx
// src/components/JsonLd.tsx

interface JsonLdProps {
  data: Record<string, unknown>
}

export function JsonLd({ data }: JsonLdProps) {
  return (
    <script
      type="application/ld+json"
      dangerouslySetInnerHTML={{ __html: JSON.stringify(data) }}
    />
  )
}
```

- [ ] **Step 2: 在首页添加 WebApplication + FAQPage schema**

在 `src/app/[locale]/page.tsx` 顶部添加 import：
```tsx
import { JsonLd } from "@/components/JsonLd"
```

在 return 的 `<main>` 标签内最前方插入：

```tsx
<JsonLd
  data={{
    "@context": "https://schema.org",
    "@type": "WebApplication",
    name: "ImgSplit",
    url: "https://imgsplit.com",
    description: t("hero.description"),
    applicationCategory: "DesignApplication",
    operatingSystem: "Any",
    offers: {
      "@type": "Offer",
      price: "0",
      priceCurrency: "USD",
    },
    featureList: [
      "Drag-and-drop split lines",
      "Snap alignment",
      "Grid splitting",
      "One-click ZIP download",
      "Browser-based processing",
      "Privacy-first design",
    ],
  }}
/>
<JsonLd
  data={{
    "@context": "https://schema.org",
    "@type": "FAQPage",
    mainEntity: [
      { "@type": "Question", name: t("faq.q1"), acceptedAnswer: { "@type": "Answer", text: t("faq.a1") } },
      { "@type": "Question", name: t("faq.q2"), acceptedAnswer: { "@type": "Answer", text: t("faq.a2") } },
      { "@type": "Question", name: t("faq.q3"), acceptedAnswer: { "@type": "Answer", text: t("faq.a3") } },
      { "@type": "Question", name: t("faq.q4"), acceptedAnswer: { "@type": "Answer", text: t("faq.a4") } },
      { "@type": "Question", name: t("faq.q5"), acceptedAnswer: { "@type": "Answer", text: t("faq.a5") } },
      { "@type": "Question", name: t("faq.q6"), acceptedAnswer: { "@type": "Answer", text: t("faq.a6") } },
    ],
  }}
/>
```

- [ ] **Step 3: 验证构建通过**

Run: `bun run build`
Expected: 构建成功

- [ ] **Step 4: Commit**

```bash
git add src/components/JsonLd.tsx src/app/[locale]/page.tsx
git commit -m "feat(seo): 添加 WebApplication 和 FAQPage JSON-LD 结构化数据"
```

---

## 实施顺序

```
Task 1 (首页 Server Component 重构) ──→ Task 6 (JSON-LD，依赖 Task 1)
                                    ↗
Task 2 (robots.ts)     ─┐
Task 3 (sitemap.ts)    ─┼── 可并行 ──→ Task 5 (OG/Twitter/canonical)
Task 4 (workspace noindex) ─┘
```

- **Task 1** 最优先，影响最大（Critical）
- **Task 2、3、4** 互相独立，可并行执行
- **Task 5** 独立于 Task 1，可随时执行
- **Task 6** 必须在 Task 1 完成后执行（依赖 Server Component 的 page.tsx）
